<%= form_for(@guide) do |f| %>
  <% if @guide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itinerary.errors.count, "error") %> prohibited this itinerary from being saved:</h2>

      <ul>
      <% @guide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :intro %><br>
    <%= f.text_area :intro, cols: 75, rows: 5 %>
  </div>

<%= f.fields_for :itineraries do |builder| %>
  <%= render 'itinerary_fields', f: builder %>

<% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
